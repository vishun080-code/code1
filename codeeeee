index.html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Verification Required</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      font-family: 'Segoe UI', sans-serif;
      overflow: hidden;
    }

    /* LOADING SCREEN */
    #loading {
      position: fixed;
      inset: 0;
      background: #0b0b0b;
      color: #fff;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      font-size: 1.4rem;
      z-index: 10;
    }

    .dots::after {
      content: '';
      animation: dots 1.5s steps(3, end) infinite;
    }

    @keyframes dots {
      0% { content: ''; }
      33% { content: '.'; }
      66% { content: '..'; }
      100% { content: '...'; }
    }

    /* MAIN PAGE */
    #main {
      display: none;
      height: 100vh;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      color: white;
      text-align: center;
      padding: 20px;
    }

    .card {
      position: relative;
      top: 50%;
      transform: translateY(-50%);
      background: rgba(255,255,255,0.15);
      padding: 35px;
      border-radius: 20px;
      max-width: 360px;
      margin: auto;
      box-shadow: 0 10px 30px rgba(0,0,0,0.25);
    }

    h1 {
      font-size: 1.9rem;
      margin-bottom: 10px;
    }

    p {
      font-size: 1.1rem;
      line-height: 1.6;
      min-height: 120px;
    }

    /* FLOATING HEARTS */
    .heart {
      position: absolute;
      color: rgba(255,255,255,0.8);
      font-size: 20px;
      animation: floatUp 6s linear infinite;
    }

    @keyframes floatUp {
      from {
        transform: translateY(100vh);
        opacity: 1;
      }
      to {
        transform: translateY(-10vh);
        opacity: 0;
      }
    }
  </style>
</head>
<body>

  <!-- LOADING -->
  <div id="loading">
    <div>Verifying device</div>
    <div class="dots"></div>
  </div>

  <!-- MAIN CONTENT -->
  <div id="main">
    <div class="card">
      <h1>💖 Happy One Month 💖</h1>
      <p id="typewriter"></p>
    </div>
  </div>

  <script>
    // FAKE LOADING
    setTimeout(() => {
      document.getElementById('loading').style.display = 'none';
      document.getElementById('main').style.display = 'block';
      startTyping();
      spawnHearts();
    }, 2500);

    // TYPEWRITER TEXT
    const text = `Hey HER_NAME_HERE 💗

This looked like a scam link,
but the real scam is how you stole my heart 😌

One month with you has been my favorite chapter.
Thank you for being you.

Happy one month anniversary, babe 💕`;

    let index = 0;
    function startTyping() {
      const el = document.getElementById("typewriter");
      const interval = setInterval(() => {
        el.innerHTML += text.charAt(index);
        index++;
        if (index >= text.length) clearInterval(interval);
      }, 40);
    }

    // FLOATING HEARTS
    function spawnHearts() {
      setInterval(() => {
        const heart = document.createElement("div");
        heart.className = "heart";
        heart.innerHTML = "💗";
        heart.style.left = Math.random() * 100 + "vw";
        heart.style.fontSize = (16 + Math.random() * 20) + "px";
        document.body.appendChild(heart);
        setTimeout(() => heart.remove(), 6000);
      }, 300);
    }
  </script>

</body>
</html>
